# vlg-clip-india
Improving CLIP zero-shot accuracy on Indian image datasets using linear probe and LoRA.

## Overview
This project evaluates CLIP's zero-shot performance on Indian cultural image datasets and improves accuracy using lightweight fine-tuning methods (Linear Probe and LoRA).

## Baseline
To reproduce zero-shot baseline:

```bash
python src/zero_shot_eval.py
